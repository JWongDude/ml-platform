# Application File Paths, will likely put into yaml file
PIPELINES_DIRPATH = "app/model/pipelines"
IMAGE_CLASSIFICATION_CLASSMAP_FILENAME = "Class Map"

""" 
4) Now, Integrate the Logging Directory. 
5) Adding a Logging Directory becomes very simple as renaming/deleting the file uses the SAME EXACT FUNCTION.

"""